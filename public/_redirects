# Netlify redirects for Single Page Application (SPA)
# This ensures that all routes are handled by React Router on the client-side

# Redirect all routes to index.html for client-side routing
/*    /index.html   200

# Optional: Redirect specific API calls or assets if needed
# /api/*  https://your-api-domain.com/api/:splat  200
